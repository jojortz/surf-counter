{
    "scripts": {
      "dev": "concurrently \"cd backend && uvicorn api.main:app --host 0.0.0.0 --port 8000 --reload\" \"cd frontend && npm run dev\""
    },
    "devDependencies": {
      "concurrently": "^8.2.0"
    }
  }